# =============================================================================
# CONFIGURAÇÃO DO BANCO DE DADOS
# =============================================================================
# URL de conexão com MongoDB (substitua pela sua string de conexão)
MONGODB_URI=mongodb://localhost:27017/bikefix

# =============================================================================
# CONFIGURAÇÃO DE AUTENTICAÇÃO JWT
# =============================================================================
# Chave secreta para assinar tokens JWT (use uma chave forte em produção)
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
# Tempo de expiração do token JWT
JWT_EXPIRES_IN=7d

# =============================================================================
# CONFIGURAÇÃO DO SERVIDOR
# =============================================================================
# Porta do servidor (Render define automaticamente)
PORT=5000
# Ambiente de execução
NODE_ENV=development
# URL do frontend para CORS
FRONTEND_URL=http://localhost:3000

# =============================================================================
# CONFIGURAÇÃO DE EMAIL (OPCIONAL)
# =============================================================================
# Configurações para envio de emails (deixe vazio para usar Ethereal em dev)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password
EMAIL_FROM=noreply@bikefix.com

# =============================================================================
# INSTRUÇÕES PARA DEPLOY NO RENDER
# =============================================================================
# 1. Configure MONGODB_URI com sua string de conexão do MongoDB Atlas
# 2. Gere uma JWT_SECRET forte (ex: openssl rand -base64 32)
# 3. Configure FRONTEND_URL com a URL do seu frontend no Render
# 4. Configure NODE_ENV=production
# 5. Configure as variáveis de email se necessário